const nav=document.getElementById("nav");const menuButton=document.getElementById("menuButton");if(menuButton&&nav){menuButton.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(open))});nav.querySelectorAll("a").forEach(link=>{link.addEventListener("click",()=>{nav.classList.remove("open");menuButton.setAttribute("aria-expanded","false")})})}
const trainingDetail=document.getElementById("trainingDetail");const trainingTabs=document.querySelectorAll(".training-tab");
function renderTrainingDetail(key){const item=window.TRAININGS[key];if(!item||!trainingDetail)return;trainingDetail.style.opacity="0";trainingDetail.style.transform="translateY(8px)";setTimeout(()=>{trainingDetail.innerHTML=`<h3>${item.title}</h3><p><strong>Dlaczego ten temat?</strong> ${item.reason}</p><div class="scope-box"><h4>Zakres szkolenia</h4><ul>${item.scope.map(point=>`<li>${point}</li>`).join("")}</ul></div><p><strong>Efekt dla uczestnika:</strong> ${item.result}</p>`;trainingDetail.style.opacity="1";trainingDetail.style.transform="translateY(0)"},140)}
trainingTabs.forEach(button=>{button.addEventListener("click",()=>{trainingTabs.forEach(item=>item.classList.remove("active"));button.classList.add("active");renderTrainingDetail(button.dataset.training)})});
function setupSignupLinks(){const links=window.SIGNUP_LINKS||{};[["fakeNewsSignup",links.fakeNews],["communicationSignup",links.communication],["gamificationSignup",links.gamification]].forEach(([id,url])=>{const button=document.getElementById(id);if(!button)return;if(url&&url!=="#"){button.href=url;button.target="_blank";button.rel="noreferrer"}else{button.addEventListener("click",event=>{event.preventDefault();alert("Formularz zapisu zostanie dodany wkrótce.")})}})}
function setupRevealAnimations(){const items=document.querySelectorAll(".reveal");const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}})},{threshold:.12});items.forEach(item=>observer.observe(item))}
renderTrainingDetail("fakeNews");setupSignupLinks();setupRevealAnimations();